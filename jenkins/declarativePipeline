pipeline {
    agent any
    stages {

        steps ('Initialize') {
            echo 'Initializing app build'
        }
        steps('Build') {
            echo 'Building app'
        }

        steps('Test') {
            echo 'Testing app'
        }
    }
}